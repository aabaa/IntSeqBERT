%% IntSeqBERT — CICM 2026 presentation (Beamer / metropolis, 16:9)
%% Compile: pdflatex slides && pdflatex slides

\documentclass[aspectratio=169,10pt]{beamer}

\usetheme[progressbar=frametitle,numbering=fraction]{metropolis}

% Blue identifies spoken sentences; emphasis uses black bold text.
\setbeamercolor{alerted text}{fg=black}
\setbeamerfont{alerted text}{series=\bfseries}
\setbeamercolor{block title alerted}{fg=black}
\setbeamerfont{block title alerted}{series=\bfseries}

\usepackage[T1]{fontenc}
\usepackage{lmodern}
\usepackage{amsmath}
\usepackage{amssymb}
\usepackage{booktabs}
\usepackage{array}
\usepackage{graphicx}
\usepackage{pgfpages}
\usepackage{tikz}
\usetikzlibrary{arrows.meta}

% Keep the 16:9 slide layout, but emit each page on A4 landscape paper.
% This also makes printer-driver n-up layouts (such as 4 pages per sheet)
% handle the document at the intended physical size.
\pgfpagesuselayout{resize to}[a4paper,landscape]

\graphicspath{{../figures/}}

% slightly tighter itemize spacing for text-rich slides
\newcommand{\tightlist}{\setlength{\itemsep}{0.2em}}

% Read the dark-blue sentences in order; bold marks emphasis within or outside them.
\definecolor{readoutblue}{HTML}{174EA6}
\newcommand{\readout}[1]{\textcolor{readoutblue}{{\mdseries #1}}}

\title{IntSeqBERT: Learning Arithmetic Structure in OEIS\texorpdfstring{\\}{ }via Modulo-Spectrum Embeddings}
\author{Kazuhisa Nakasho}
\institute{Iwate Prefectural University, Japan\\[1ex]
  Code \& data: \texttt{github.com/aabaa/IntSeqBERT}}
\date{CICM 2026}

\begin{document}

% Use the title components without the theme's fixed paper-height minipage.
\begin{frame}[plain]
  \usebeamertemplate*{title}
  \usebeamertemplate*{title separator}
  \usebeamertemplate*{author}
  \usebeamertemplate*{date}
  \usebeamertemplate*{institute}
  \medskip
  {\small\readout{Today I will introduce IntSeqBERT, a model that learns arithmetic patterns in integer sequences.}}
\end{frame}

\begin{frame}{Why Finding Good Problems Matters}
\small
\setlength{\parskip}{0pt}
  \textbf{A recent Mathstodon thread by Terence Tao}\hfill{\footnotesize 8 September 2026}
  \par\smallskip
  {\scriptsize Summary of the thread, with context from his earlier posts on AI evaluation.}
  \begin{itemize}\tightlist
    \item \readout{Terence Tao worries that AI may use up good open problems
          as benchmarks without helping us create new ones.}
    \item \readout{Studying good problems helps us \textbf{understand more and find connections to other questions}.}
    \item \readout{He warns that focusing only on answers may harm this process and make it harder
          to develop \textbf{new ideas and good problems}.}
  \end{itemize}
  \smallskip
  \begin{quote}\footnotesize
    ``reveal any further insights or connections to other questions''
  \end{quote}
  {\footnotesize His example: there are many possible questions, but few useful ones,
  just as there is plenty of sea water but not always enough drinking water.}
  \par\medskip
  {\scriptsize Sources (paraphrased above; excerpt from post 1/4):\\
    \href{https://mathstodon.xyz/@tao/117237320796901560}{Mathstodon, 8 September 2026: fruitful open problems, insights, and the research ecosystem (thread 1--4).}\\
    \href{https://mathstodon.xyz/@tao/117204929023813310}{Related thread: AI evaluation and open problems as a non-renewable resource.}}
\end{frame}

\begin{frame}{Long-Term Goal --- AI for Mathematical Discovery}
\small
  \begin{itemize}\tightlist
    \item \readout{Our goal is to help find new problems by looking for connections in numerical patterns.}
      \begin{itemize}
        \item McKay noticed $196{,}884 = 196{,}883 + 1$; this led to \emph{Monstrous Moonshine}.
        \item \readout{The \textbf{Taniyama--Shimura conjecture} links elliptic curves and modular forms.
              This link was key to the proof of \textbf{Fermat's Last Theorem}.}
              {\footnotesize\href{https://annals.math.princeton.edu/1995/141-3/p01}{Wiles (1995): the semistable case.}}
        \item The BSD conjecture and mirror symmetry also began with numerical patterns
              ($317{,}206{,}375$ rational curves in the mirror symmetry example).
      \end{itemize}
    \item AI now takes part in this kind of discovery. Examples include
          \textbf{AlphaProof}, the \textbf{Ramanujan Machine}, and \textbf{FunSearch}.
    \item \readout{We want to find sequences with similar arithmetic patterns
          and use those similarities to suggest conjectures.}
          The sequences may come from different fields of mathematics.
    \item \readout{We use the \textbf{OEIS}, the Online Encyclopedia of Integer Sequences.}
          Our snapshot contains 391{,}710 sequences.
    \item \readout{To work towards that goal, we first need a model that captures arithmetic structure.}
          We then measure how much structure it learns.
  \end{itemize}
\end{frame}

\begin{frame}{Multiplication --- Theory and Practice}
\footnotesize
  \setlength{\parskip}{0pt}
  \readout{For such a model, multiplication is a challenge: simple in theory, but hard to learn.}
  \par\smallskip
  {\setlength{\tabcolsep}{4pt}\renewcommand{\arraystretch}{1.1}
  \begin{tabular}{@{}>{\raggedright\arraybackslash}p{0.32\textwidth}>{\raggedright\arraybackslash}p{0.64\textwidth}@{}}
    \toprule
    \textbf{Setting and study} & \textbf{Main result} \\
    \midrule
    \textbf{Theory: threshold circuits}\newline
    \href{https://doi.org/10.1137/S0895480192228619}{Siu \& Roychowdhury (1994)}
      & \readout{In theory, multiplication can be implemented with just \textbf{3 layers}.}\newline
        Polynomial-size threshold circuits; depth 3 is optimal in this class. \\
    \addlinespace[0.3em]
    \textbf{Practice: trained Transformer}\newline
    \href{https://arxiv.org/abs/2311.14737}{Shen et al.\ (2023), Sec.\ 2 / Table 5}
      & \readout{In practice, even a \textbf{12-layer model} got only \textbf{84\%} of 15-digit multiplications right.}\newline
        $15{\times}15$-digit task; zero-padding and reversed product digits. \\
    \addlinespace[0.3em]
    \textbf{GPT-4: decimal multiplication}\newline
    Dziri et al.\ (2023)
      & Accuracy falls from \textbf{59\%} for $3{\times}3$-digit products
        to \textbf{4\%} for $4{\times}4$-digit products. \\
    \addlinespace[0.3em]
    \textbf{2-layer Transformer: mod $p$}\newline
    Power et al.\ (2022)
      & Generalisation appears only after prolonged training (\textbf{grokking}). \\
    \bottomrule
  \end{tabular}}
  \par\smallskip
  {\scriptsize Different model classes and tasks; the experiment does not establish a minimum depth.}\par\smallskip
  \readout{Having enough layers does not guarantee that the model will learn multiplication.}
  \par\smallskip
  \readout{For OEIS sequences, input representation is another challenge: a fixed vocabulary cannot cover all integers.}\\
  Token IDs hide arithmetic structure; out-of-vocabulary values share \texttt{UNK}.
  OEIS values can reach $10^{210}$.
\end{frame}

\begin{frame}{Key Idea --- Show Multiplicative Structure in the Input}
\small
  \begin{itemize}\tightlist
    \item \readout{To address these challenges, we represent each integer by its remainders under several moduli.}
          \readout{These remainders, or residues, \textbf{preserve addition and multiplication}.}
          Reduction mod $m$ is a \alert{ring homomorphism}:
          $\mathbb{Z} \to \mathbb{Z}/m\mathbb{Z}$.
    \item \readout{The residues stay small, even when the original values are very large.}
          For $10^{210}$, a residue is still in $\{0,\dots,m-1\}$.
          The sequence $(x_i \bmod m)$ keeps multiplicative structure.
    \item Classical number theory explains these \textbf{periodic patterns}:
      \begin{itemize}
        \item \textbf{Fermat's little theorem} describes powers mod $p$
              (the period divides $p-1$).
        \item \textbf{Quadratic reciprocity} links residue information across primes.
        \item The \textbf{CRT} combines information from several prime powers.
      \end{itemize}
    \item \readout{We call this the \textbf{modulo spectrum} and expect it to make arithmetic patterns easier to learn.}
          It exposes multiplicative structure \alert{at depth 0}, before the network learns it across layers.
  \end{itemize}
\end{frame}

\begin{frame}{What Does the Modulo Spectrum Reveal?}
\small
  \readout{Powers of 2 and squares give repeating patterns modulo 3 and 5.}
  \par\smallskip
  \centering
  \begin{tikzpicture}[x=1cm,y=1cm,
    cycle/.style={rounded corners=2pt,draw=mDarkTeal!30,fill=mDarkTeal!7},
    labeltext/.style={anchor=west,font=\footnotesize}]
    \useasboundingbox (0,0) rectangle (14,4.65);
    \node[labeltext,font=\scriptsize] at (0,4.5) {$n=0,1,\ldots,9$ in both examples};
    \node[labeltext] at (0,3.98) {$2^n$};
    \node[font=\small] at (2.85,3.98) {$1$};
    \node[font=\small] at (3.75,3.98) {$2$};
    \node[font=\small] at (4.65,3.98) {$4$};
    \node[font=\small] at (5.55,3.98) {$8$};
    \node[font=\small] at (6.45,3.98) {$16$};
    \node[font=\small] at (7.35,3.98) {$32$};
    \node[font=\small] at (8.25,3.98) {$64$};
    \node[font=\small] at (9.15,3.98) {$128$};
    \node[font=\small] at (10.05,3.98) {$256$};
    \node[font=\small] at (10.95,3.98) {$512$};
    \node at (11.7,3.98) {$\cdots$};
    \node[labeltext] at (0,3.4) {Modulo 3};
    \draw[cycle] (2.45,3.19) rectangle +(1.70,0.42);
    \draw[cycle] (4.25,3.19) rectangle +(1.70,0.42);
    \draw[cycle] (6.05,3.19) rectangle +(1.70,0.42);
    \draw[cycle] (7.85,3.19) rectangle +(1.70,0.42);
    \draw[cycle] (9.65,3.19) rectangle +(1.70,0.42);
    \node[labeltext] at (12,3.4) {Period 2};
    \node[font=\small] at (2.85,3.4) {$1$};
    \node[font=\small] at (3.75,3.4) {$2$};
    \node[font=\small] at (4.65,3.4) {$1$};
    \node[font=\small] at (5.55,3.4) {$2$};
    \node[font=\small] at (6.45,3.4) {$1$};
    \node[font=\small] at (7.35,3.4) {$2$};
    \node[font=\small] at (8.25,3.4) {$1$};
    \node[font=\small] at (9.15,3.4) {$2$};
    \node[font=\small] at (10.05,3.4) {$1$};
    \node[font=\small] at (10.95,3.4) {$2$};
    \node at (11.7,3.4) {$\cdots$};
    \node[labeltext] at (0,2.82) {Modulo 5};
    \draw[cycle] (2.45,2.61) rectangle +(3.50,0.42);
    \draw[cycle] (6.05,2.61) rectangle +(3.50,0.42);
    \node[labeltext] at (12,2.82) {Period 4};
    \node[font=\small] at (2.85,2.82) {$1$};
    \node[font=\small] at (3.75,2.82) {$2$};
    \node[font=\small] at (4.65,2.82) {$4$};
    \node[font=\small] at (5.55,2.82) {$3$};
    \node[font=\small] at (6.45,2.82) {$1$};
    \node[font=\small] at (7.35,2.82) {$2$};
    \node[font=\small] at (8.25,2.82) {$4$};
    \node[font=\small] at (9.15,2.82) {$3$};
    \node[font=\small] at (10.05,2.82) {$1$};
    \node[font=\small] at (10.95,2.82) {$2$};
    \node at (11.7,2.82) {$\cdots$};
    \node[labeltext] at (0,1.84) {$n^2$};
    \node[font=\small] at (2.85,1.84) {$0$};
    \node[font=\small] at (3.75,1.84) {$1$};
    \node[font=\small] at (4.65,1.84) {$4$};
    \node[font=\small] at (5.55,1.84) {$9$};
    \node[font=\small] at (6.45,1.84) {$16$};
    \node[font=\small] at (7.35,1.84) {$25$};
    \node[font=\small] at (8.25,1.84) {$36$};
    \node[font=\small] at (9.15,1.84) {$49$};
    \node[font=\small] at (10.05,1.84) {$64$};
    \node[font=\small] at (10.95,1.84) {$81$};
    \node at (11.7,1.84) {$\cdots$};
    \node[labeltext] at (0,1.26) {Modulo 3};
    \draw[cycle] (2.45,1.05) rectangle +(2.60,0.42);
    \draw[cycle] (5.15,1.05) rectangle +(2.60,0.42);
    \draw[cycle] (7.85,1.05) rectangle +(2.60,0.42);
    \node[labeltext] at (12,1.26) {Period 3};
    \node[font=\small] at (2.85,1.26) {$0$};
    \node[font=\small] at (3.75,1.26) {$1$};
    \node[font=\small] at (4.65,1.26) {$1$};
    \node[font=\small] at (5.55,1.26) {$0$};
    \node[font=\small] at (6.45,1.26) {$1$};
    \node[font=\small] at (7.35,1.26) {$1$};
    \node[font=\small] at (8.25,1.26) {$0$};
    \node[font=\small] at (9.15,1.26) {$1$};
    \node[font=\small] at (10.05,1.26) {$1$};
    \node[font=\small] at (10.95,1.26) {$0$};
    \node at (11.7,1.26) {$\cdots$};
    \node[labeltext] at (0,0.68) {Modulo 5};
    \draw[cycle] (2.45,0.47) rectangle +(4.40,0.42);
    \draw[cycle] (6.95,0.47) rectangle +(4.40,0.42);
    \node[labeltext] at (12,0.68) {Period 5};
    \node[font=\small] at (2.85,0.68) {$0$};
    \node[font=\small] at (3.75,0.68) {$1$};
    \node[font=\small] at (4.65,0.68) {$4$};
    \node[font=\small] at (5.55,0.68) {$4$};
    \node[font=\small] at (6.45,0.68) {$1$};
    \node[font=\small] at (7.35,0.68) {$0$};
    \node[font=\small] at (8.25,0.68) {$1$};
    \node[font=\small] at (9.15,0.68) {$4$};
    \node[font=\small] at (10.05,0.68) {$4$};
    \node[font=\small] at (10.95,0.68) {$1$};
    \node at (11.7,0.68) {$\cdots$};
    \draw[mDarkTeal!20] (0,2.35) -- (14,2.35);
  \end{tikzpicture}
  \par\smallskip
  \readout{Modulo 3, powers of 2 repeat every 2 terms, while squares repeat every 3.}
  \par\smallskip
  \readout{These different patterns give the model clues about each sequence's arithmetic structure.}
\end{frame}

\begin{frame}{Task --- Masked Sequence Modelling on OEIS}
\small
  \begin{itemize}\tightlist
    \item \readout{To test this idea, we hide sequence terms and ask the model to predict them.}
          This is \alert{masked sequence modelling}: BERT-style learning for integers.
    \item We take up to the first \textbf{128 terms} and mask \textbf{15\%} of the positions.
          The remaining terms provide the context.
    \item \readout{We also test next-term prediction, where the model sees only the preceding terms.}
          Only the last term is masked in this setting.
    \item The \textbf{FACT} benchmark (NeurIPS 2022) tested a plain Transformer on 5 OEIS
          tasks. Unmasking was the hardest task.
          Our \textbf{Vanilla baseline} uses that architecture.
    \item \readout{For each hidden term, we predict its \textbf{magnitude, sign, and residues}.}
      \begin{enumerate}
        \item \readout{Magnitude measures size on a logarithmic scale.} $v_i = 1 + \log_{10}|x_i|$.
        \item \readout{Sign is positive, negative, or 0.} $s_i \in \{+, -, 0\}$.
        \item \readout{We predict residues for 100 moduli, from 2 to 101.} $x_i \bmod m$, $m \in \{2, \dots, 101\}$.
      \end{enumerate}
  \end{itemize}
\end{frame}

\begin{frame}{A Worked Example --- Predicting a Missing Integer}
\centering
\begin{tikzpicture}[x=1cm,y=1cm,
  flow/.style={-{Stealth[length=2mm]},thick,draw=mDarkTeal},
  term/.style={rounded corners=2pt,fill=mDarkTeal!8,
    minimum width=0.85cm,minimum height=0.48cm,font=\large},
  card/.style={rounded corners=3pt,draw=mDarkTeal!30,fill=mDarkTeal!4,
    text width=3.65cm,minimum height=1.12cm,align=center,font=\footnotesize}]
  \useasboundingbox (0,0) rectangle (14,6.25);
  \node[anchor=west,font=\small\bfseries] at (0,6.05)
    {\readout{Here we hide 16 in the powers of 2 and use the surrounding terms as context.}};
  \foreach \entry [count=\i from 0] in {1,2,4,8,?,32,64} {
    \ifnum\i=4
      \node[term,fill=mLightBrown!20,draw=mLightBrown,thick] (t\i)
        at ({3.1+1.15*\i},5.45) {$\entry$};
    \else
      \node[term] (t\i) at ({3.1+1.15*\i},5.45) {$\entry$};
    \fi
  }
  \node[anchor=west,font=\footnotesize] at (0,4.88) {Visible terms mod 3:};
  \foreach \entry [count=\i from 0] in {1,2,1,2,?,2,1} {
    \node[font=\footnotesize,text=mDarkTeal] at ({3.1+1.15*\i},4.88) {$\entry$};
  }
  \node[anchor=west,align=left,font=\scriptsize\bfseries,text=black] at (10.7,4.88)
    {A simple periodic\\pattern};
  \draw[flow] (7,4.62) -- (7,4.33);
  \node[rounded corners=3pt,fill=mDarkTeal,text=white,
    minimum width=4.4cm,minimum height=0.48cm,font=\small\bfseries]
    (model) at (7,4.05) {IntSeqBERT};
  \node[anchor=west,align=left,font=\small\bfseries] at (0,4.05)
    {\readout{The model predicts}\\\readout{3 properties.}};
  \node[card] (mag) at (2.25,2.70)
    {\textbf{Magnitude}\\$1+\log_{10}16 \approx 2.204$\\
     {\scriptsize Model: size + uncertainty}};
  \node[card] (sign) at (7,2.70)
    {\textbf{Sign}\\$+$\\
     {\scriptsize Model: class probabilities}};
  \node[card] (res) at (11.75,2.70)
    {\textbf{Residues} {\scriptsize ($m=2,\ldots,101$)}\\
     {\scriptsize mod 2: 0; mod 3: 1; mod 5: 1}\\
     {\scriptsize Model: residue probabilities}};
  \draw[flow] (model.south) -- (7,3.55) -| (mag.north);
  \draw[flow] (model.south) -- (7,3.55) -| (res.north);
  \draw[flow] (model.south) -- (sign.north);
  \node[rounded corners=3pt,draw=mDarkTeal,fill=mDarkTeal!8,
    minimum width=3.4cm,minimum height=0.52cm,font=\small]
    (solver) at (7,1.04) {CRT Solver};
  \draw[flow] (mag.south) -- (2.25,1.48) -| (solver.north);
  \draw[flow] (sign.south) -- (solver.north);
  \draw[flow] (res.south) -- (11.75,1.48) -| (solver.north);
  \node[anchor=west,align=left,font=\small\bfseries] at (0,0.97)
    {\readout{The Solver combines them}\\\readout{to recover an integer.}};
  \node[term,fill=mLightBrown!20,draw=mLightBrown,minimum width=1.1cm]
    (answer) at (10.6,1.04) {$16$};
  \draw[flow] (solver.east) -- (answer.west);
  \node[anchor=west,align=left,font=\scriptsize] at (11.4,1.04)
    {Example with\\correct predictions};
  \node[font=\scriptsize\bfseries,text=black] at (7,0.43)
    {\readout{Here, correct predictions allow the Solver to recover 16.}};
  \node[font=\scriptsize,align=center] at (7,0.06)
    {\textbf{Training:} compare the 3 predictions with the hidden term's targets
     $\;\longrightarrow\;$ update IntSeqBERT.};
\end{tikzpicture}
\end{frame}

\begin{frame}{The Whole Pipeline in 1 Slide}
\small
  \readout{In the full system, the model predicts a missing term's properties, and the Solver recovers its value.}
  \par\medskip
  \centering
  \begin{tikzpicture}[x=1cm,y=1cm,
    flow/.style={-{Stealth[length=2mm]},thick,draw=mDarkTeal},
    card/.style={rounded corners=3pt,draw=mDarkTeal!30,fill=mDarkTeal!4,
      text width=3.5cm,minimum height=1.05cm,align=center,font=\small}]
    \useasboundingbox (0,0) rectangle (14,4.25);
    \node[card] (input) at (2,3.5)
      {\textbf{OEIS sequence}\\{\footnotesize With a hidden term}};
    \node[card] (features) at (7,3.5)
      {\textbf{Magnitude +}\\\textbf{Modulo Spectrum}};
    \node[card,fill=mDarkTeal,text=white] (model) at (12,3.5)
      {\textbf{IntSeqBERT}};
    \node[card,minimum height=1.35cm] (heads) at (12,1.15)
      {\textbf{Magnitude / Sign / Residues}\\
       {\footnotesize Size + uncertainty\\Sign / residue probabilities}};
    \node[card,draw=mDarkTeal,fill=mDarkTeal!8] (solver) at (7,1.15)
      {\textbf{Probabilistic CRT Solver}\\{\footnotesize Combine and score candidates}};
    \node[card,draw=mLightBrown,fill=mLightBrown!20] (output) at (2,1.15)
      {\textbf{Integer prediction}};
    \draw[flow] (input) -- (features);
    \draw[flow] (features) -- (model);
    \draw[flow] (model) -- (heads);
    \draw[flow] (heads) -- (solver);
    \draw[flow] (solver) -- (output);
  \end{tikzpicture}
  \par\smallskip
  {\footnotesize\readout{The Solver uses a \textbf{probabilistic} approach based on the Chinese Remainder Theorem, or CRT. It uses the predicted probabilities to score candidate integers.}}
\end{frame}

\begin{frame}{IntSeqBERT --- Dual-Stream Architecture}
\setlength{\parskip}{0pt}
\centering
  \includegraphics[width=0.58\textwidth]{fig1_architecture.png}
  \begin{itemize}\tightlist\footnotesize
    \item \readout{Let us look at the neural network architecture. One stream encodes magnitude and sign; the other encodes residues.}
          The magnitude stream sends log-scale size and sign ($\mathbb{R}^4$) through an MLP.
    \item \readout{The modulo stream places residues on a circle using sine and cosine.}
          For the $\mathbb{R}^{200}$ input,
          $\phi_m(r) = [\sin(2\pi r/m), \cos(2\pi r/m)]$.
          Residues $m{-}1$ and $0$ stay neighbours.
    \item \readout{We then combine these 2 streams and pass the result to a Transformer.}
          With \textbf{FiLM}, the modulo stream scales and shifts the magnitude stream:
          $\mathbf{e}_i = (1 + \boldsymbol{\gamma}_i) \odot \mathbf{h}_i^{\mathrm{mag}} + \boldsymbol{\beta}_i$.
    \item \readout{We test 3 model sizes: Small, Middle, and Large.}
          Each uses a standard pre-LN Transformer encoder.
  \end{itemize}
  {\footnotesize\setlength{\tabcolsep}{8pt}
  \begin{tabular}{@{}lcccr@{}}
    \toprule
    \textbf{Model size} & Layers & Hidden dim. $d$ & Attention heads & Parameters \\
    \midrule
    Small  & 6  & 256 & 4  & 6.4M \\
    Middle & 8  & 512 & 8  & 29.0M \\
    Large  & 12 & 768 & 12 & 91.5M \\
    \bottomrule
  \end{tabular}\par}
  {\footnotesize Model sizes describe network capacity; value buckets describe $|x|$.\par}
\end{frame}

\begin{frame}{Training --- Multi-Task Objective}
\small
  \begin{itemize}\tightlist
    \item \readout{We train these models to predict all 3 outputs together, including uncertainty in magnitude.}
      \begin{itemize}
        \item The \textbf{magnitude head} predicts size and its \alert{uncertainty},
              $(\mu_i, \sigma_i^2)$. The Solver uses both.
        \item The \textbf{sign head} predicts 1 of 3 classes.
        \item The \textbf{modulo head} has 100 independent classifiers.
              Each loss is normalised by the maximum entropy $\ln m$,
              so mod 2 and mod 101 contribute fairly.
      \end{itemize}
    \item We combine the losses with these weights:
          $\mathcal{L} = \mathcal{L}_{\mathrm{mag}} + \mathcal{L}_{\mathrm{sign}} + 2\,\mathcal{L}_{\mathrm{mod}}$.
    \item \readout{Training uses about 220,000 OEIS sequences over 200 epochs.}
          The exact training count is \textbf{219{,}765}. We use \textbf{FP32} for training.
    \item \readout{All experiments run on a \textbf{single consumer GPU}.}
          We use an RTX 3070\,Ti with 8\,GB, so other researchers can repeat the experiments.
  \end{itemize}
\end{frame}

\begin{frame}{Neural-Symbolic Decoding --- the CRT Solver}
\small
  \setlength{\parskip}{0pt}
  \begin{itemize}\small\tightlist
    \item \readout{The Solver first generates candidates in the predicted magnitude range.}
          It searches the $3\sigma$ interval, using its size to choose a method:
      \begin{itemize}
        \item \textbf{Dense} ($\Delta n \le 10^6$) checks every integer in the interval.
        \item \textbf{Sieve} ($\le 10^{14}$) uses CRT beam search with confident residues.
        \item \textbf{CRT} ($> 10^{14}$) generates large candidates directly with sparse CRT.
      \end{itemize}
    \item \readout{Next, it scores each candidate using the magnitude estimate and all 100 residue distributions.}
          For each $m$, it uses $P_m(n \bmod m)$.
  \end{itemize}
  \textbf{Probabilistic CRT: a weighted sum of log-probabilities.}
  {
    \setlength{\abovedisplayskip}{3pt}
    \setlength{\belowdisplayskip}{3pt}
    \[
      S(n) = \underbrace{-\frac{(v_n-\mu)^2}{2\sigma^2}}_{\text{magnitude log-likelihood}}
           + 0.3\underbrace{\sum_{m=2}^{101}\ln P_m(n\bmod m)}_{\text{all modulo log-probabilities}},
      \qquad v_n=1+\log_{10}n
    \]
    \footnotesize
    $P_m(r)$ is a residue probability. The weight 0.3 limits the effect of shared information.
    }
  \begin{itemize}\small\tightlist
    \item \readout{Finally, it returns the candidates with the highest scores.}
          It takes the top $k$ positive candidates and applies the sign; a zero sign returns 0.
  \end{itemize}
\end{frame}

\begin{frame}{Dataset --- 274{,}705 OEIS Sequences}
\footnotesize
\setlength{\parskip}{0pt}
  \begin{itemize}\tightlist
    \item \readout{To evaluate the full system, we use about 70\% of the OEIS, split into training, validation, and test sets.}
          From 391{,}710 entries, we retain \alert{274{,}705 (70.1\%)} and split them 8:1:1.
    \item \readout{The excluded tags concern notation, task suitability, or data quality.}
      \begin{itemize}
        \item 5 tags depend on \textbf{how values are written}:
              \texttt{cons}, \texttt{cofr}, \texttt{frac}, \texttt{base}, \texttt{word}.
        \item 2 tags \textbf{do not fit this task}: \texttt{fini}, \texttt{tabl}.
        \item 4 are \textbf{quality labels}:
              \texttt{dead}, \texttt{unkn}, \texttt{less}, \texttt{dumb}.
      \end{itemize}
    \item These choices define our task. The labels \texttt{less}/\texttt{dumb} are subjective,
          and \texttt{tabl} could be converted into sequences. Future benchmarks may choose differently.
          \hyperlink{backup-oeis-representation}{\beamergotobutton{Tag definitions and examples}}
    \item \readout{We compare performance by grouping terms by absolute value $|x|$.}
          These value buckets are separate from model sizes.
  \end{itemize}
  {\centering\footnotesize\setlength{\tabcolsep}{12pt}\renewcommand{\arraystretch}{0.95}
  \begin{tabular}{@{}ll@{}}
    \toprule
    \textbf{Value bucket} & \textbf{Range of each term $x$} \\
    \midrule
    Small        & $0 \le |x| < 10^2$ \\
    Medium       & $10^2 \le |x| < 10^5$ \\
    Large        & $10^5 \le |x| < 10^{20}$ \\
    Huge         & $10^{20} \le |x| < 10^{50}$ \\
    Astronomical & $|x| \ge 10^{50}$ \\
    \bottomrule
  \end{tabular}\par}
\end{frame}

\begin{frame}{Metrics --- and 3 Questions}
  \footnotesize
  \setlength{\parskip}{0pt}
  \readout{On the test data, we measure both the predicted properties and the final integer.}\\[0.3em]
  {\setlength{\tabcolsep}{3pt}\renewcommand{\arraystretch}{1.05}
  \begin{tabular}{@{}>{\raggedright\arraybackslash}p{0.23\textwidth}>{\raggedright\arraybackslash}p{0.43\textwidth}>{\raggedright\arraybackslash}p{0.30\textwidth}@{}}
    \toprule
    \textbf{Metric} & \textbf{Definition} & \textbf{What it measures} \\
    \midrule
    \textbf{Magnitude Accuracy}\newline ($\mathrm{Acc}_{0.5}$)
      & \readout{Magnitude accuracy measures approximate size.}\newline Within $0.5$ on the log scale
      & Approximate size; helps set the Solver's search range \\
    \addlinespace[0.2em]
    \textbf{MMA}\newline (mean modulo accuracy)
      & \readout{Mean modulo accuracy, or MMA, measures residue prediction accuracy.}\newline Averaged over all 100 moduli
      & Accuracy of residue predictions \\
    \addlinespace[0.2em]
    \textbf{NIG}\newline (normalised information gain)
      & \readout{Normalised information gain, or NIG, measures the gain over uniform prediction.}\newline
        $1-\mathcal{L}_{\mathrm{CE}}^{(m)}/\ln m$; 0 = no gain, 1 = perfect
      & Periodic structure learned for each modulus \\
    \addlinespace[0.2em]
    \textbf{Solver Top-$k$}
      & \readout{Top-$k$ tests whether the correct integer is among the first $k$ candidates.}
      & \alert{Exact integer prediction} \\
    \bottomrule
  \end{tabular}}
  \smallskip

  \textbf{For magnitude accuracy, an error within a factor of $\sqrt{10}\approx3.16$ is allowed.}\\
  True value $10^6$ $\to$ predictions in about
  $[3.2\times10^5,\ 3.2\times10^6]$ count as correct.
  \smallskip

  \readout{We use these metrics to test overall gains, gains by integer size, and exact prediction.}\\
  Q1: overall gains. \quad Q2: gains by value bucket. \quad Q3: exact prediction.
\end{frame}

\begin{frame}{What Should Happen If Our Idea Works?}
\small
  \readout{Our baseline is \textbf{Vanilla}, a Transformer based on FACT.}
  \par\medskip
  {\setlength{\tabcolsep}{6pt}\renewcommand{\arraystretch}{1.6}
  \begin{tabular}{@{}r
    >{\raggedright\arraybackslash}p{0.66\textwidth}
    >{\raggedright\arraybackslash}p{0.23\textwidth}@{}}
    \toprule
    & \textbf{Expected result} & \textbf{Where we test it} \\
    \midrule
    \alert{1} & \readout{IntSeqBERT should outperform Vanilla.}
      & Q1: overall results \\
    \alert{2} & \readout{The advantage should be larger for large integers.}\newline
      Ordinary tokenisation becomes problematic there.
      & Q2: value buckets \\
    \alert{3} & \readout{Learned residue representations should show number-theoretic structure.}
      & NIG spectrum and totient ratio \\
    \bottomrule
  \end{tabular}}
\end{frame}

\begin{frame}{Q1 --- Does It Help? Yes, Consistently}
  \small
  \setlength{\parskip}{0pt}
\begin{center}\small
    \begin{tabular}{llrrr}
      \toprule
      Model size & Model & Mag Acc & Sign Acc & MMA \\
      \midrule
      Large & \textbf{IntSeqBERT} & \textbf{95.85} & \textbf{98.54} & \textbf{50.38} \\
      Large & Vanilla             & 86.97          & 97.66          & 45.85 \\
      Large & Ablation (no modulo)& 89.70          & 98.29          & 35.22 \\
      \bottomrule
    \end{tabular}
  \end{center}

  \begin{itemize}\tightlist
    \item \readout{The dual stream improves both magnitude and residue prediction over Vanilla.}
          The Large model reaches \alert{95.85\%} magnitude accuracy (\textbf{+8.9\,pt} over Vanilla)
          and \alert{50.38\%} MMA (\textbf{+4.5\,pt}).
    \item \readout{For the Large model, removing the modulo stream lowers residue accuracy by about 15 percentage points.}
          In the ablation, MMA falls by \alert{15.2\,pt} and magnitude accuracy by \textbf{6.2\,pt}.
          Residues limit the possible integer values, helping both predictions.
    \item \readout{The same pattern holds for the Small and Middle models.}
  \end{itemize}
\end{frame}

\begin{frame}{Full Results \& Scaling Behaviour}
\centering
  \includegraphics[width=0.75\textwidth]{fig3_scaling.pdf}
  \par{\footnotesize\textbf{Model size:} Small $\to$ Middle $\to$ Large}\par
  \begin{itemize}\setlength{\itemsep}{0.15em}\footnotesize
    \item For MMA, the ranking is always IntSeqBERT $>$ Vanilla $>$ Ablation.
    \item \readout{From Small to Large, residue accuracy gains about 10 percentage points; magnitude accuracy gains only 1 percentage point.}
          From Small to Large, MMA gains \textbf{10.0\,pt}; Mag Acc gains \textbf{1.1\,pt}.
    \item Solver Top-1 also gains \textbf{5.0\,pt}: 14.05 $\to$ 19.09.
    \item We see no clear sign of overfitting: Train 1.00 / Val 1.01 at epoch 200.
          We plan larger-scale retraining.
  \end{itemize}
\end{frame}

\begin{frame}{Q2 --- Where? Exactly Where Tokenisation Breaks}
  \centering
  \includegraphics[width=0.78\textwidth]{fig5_magnitude_scatter.pdf}
  \begin{columns}[T]
    \begin{column}{0.34\textwidth}
      \centering\scriptsize
      \textbf{Prediction error (MSE)}\\[0.3em]
      \setlength{\tabcolsep}{3pt}
      \begin{tabular}{lrrr}
        \toprule
        Value bucket & IntSeq & Vanilla & Abl. \\
        \midrule
        Small & 0.111          & 0.138 & \textbf{0.103} \\
        Large & \textbf{0.162} & 2.100 & 0.381 \\
        Huge  & \textbf{2.082} & 22.73 & 5.021 \\
        \bottomrule
      \end{tabular}
    \end{column}
    \begin{column}{0.62\textwidth}
      \footnotesize
      \begin{itemize}\setlength{\itemsep}{0.2em}
        \item \readout{Turning to integer size, Vanilla's mean squared magnitude error in the Large bucket is about 13 times ours.}
              Vanilla uses \texttt{UNK} beyond its vocabulary. In the Large bucket, its MSE is \alert{13$\times$ ours}.
              Overall $R^2$ is \alert{0.988} for IntSeqBERT and 0.943 for Vanilla.
        \item \readout{Overall scores can hide this gap because many sequences grow slowly.}
              Many OEIS sequences grow slowly, so easier cases strongly affect those scores.
      \end{itemize}
    \end{column}
  \end{columns}
\end{frame}

\begin{frame}{Q3 --- The Strictest Test: Exact Next-Term Prediction}
\small
  \begin{center}\small
    \begin{tabular}{llrr}
      \toprule
      Model size & Model & Top-1 & Top-10 \\
      \midrule
      Large & \textbf{IntSeqBERT} & \textbf{19.09} & \textbf{26.23} \\
      Large & Vanilla             & 2.59           & 3.80 \\
      Large & Ablation            & 11.75          & 21.79 \\
      \bottomrule
    \end{tabular}
  \end{center}

  \begin{itemize}\tightlist
    \item \readout{Next, we test whether the model can predict the exact next term. We use 10,000 sequences kept separate from training.}
          For each sequence, we hide the last term and predict it from the earlier terms.
    \item \readout{The top-ranked answer is correct in \textbf{19.09\%} of cases, versus \textbf{2.59\%} for Vanilla.}
          \readout{This is a \textbf{7.4-fold improvement}.}
    \item Top-1 accuracy is \textbf{68.3\%} in the Small value bucket and \textbf{20.8\%} in Medium.
          Vanilla scores \textbf{0\%} beyond its vocabulary.
    \item For the Large bucket and above, exact accuracy is near 0 at MMA $\approx 50\%$.
  \end{itemize}
\end{frame}

\begin{frame}{What Does 19\% Mean? --- Better, but Still Low}
\begin{itemize}\tightlist
    \item \readout{Despite this improvement, only about 1 in 5 predictions is exact.}
    \item \readout{For large integers, exact accuracy is still \textbf{near 0}.}
          This includes the Large, Huge, and Astronomical value buckets.
  \end{itemize}
  \begin{alertblock}{What this tells us}
    \readout{Recovering an exact integer from many uncertain residue predictions remains the main difficulty.}
  \end{alertblock}
\end{frame}

\begin{frame}{But What Did the Network Learn? The NIG Spectrum}
\centering
  \includegraphics[width=0.72\textwidth]{fig2_nig_spectrum.pdf}
  \begin{itemize}\tightlist\footnotesize
    \item \readout{We also want to understand the patterns the model has learned. This plot shows information gain for each modulus.}
          NIG measures this for each modulus from 2 to 101, beyond overall accuracy.
    \item \readout{IntSeqBERT achieves higher information gain at every modulus, with a clear pattern of peaks.}
          It is above both baselines across the \textbf{entire NIG spectrum}.
    \item \readout{The highest peak is at modulus 96, followed by modulus 2.}
          This holds across all models and sizes: $96 = 2^5 \times 3$ (NIG\,$=0.629$).
          Mod 2 captures \textbf{parity}, which is common in the OEIS.
    \item Mod-2 accuracy is \textbf{85.7} for IntSeqBERT, 81.4 for Vanilla, and 72.1 for Ablation.
  \end{itemize}
\end{frame}

\begin{frame}{Finding --- Higher NIG with a Lower Totient Ratio}
\begin{columns}[T]
    \begin{column}{0.46\textwidth}
      \centering
      \includegraphics[width=\textwidth]{fig2b_nig_vs_phi.pdf}
    \end{column}
    \begin{column}{0.54\textwidth}
      \footnotesize
      \setlength{\parskip}{0pt}
      \textbf{Observation}\\
      \readout{The pattern is related to the factors of each modulus. We measure this using the totient ratio. A lower ratio tends to go with higher information gain.}\\
      The totient ratio is the fraction of residues coprime to $m$:\\
      $\varphi(m)/m = \prod_{p \mid m}(1 - 1/p)$.\\
      Pearson \alert{$r = -0.851$} ($p < 10^{-28}$).\\
      \readout{This is a link found in our experiments, not a proven rule.}
      \par\vspace{1pt}

      \textbf{Interpretation --- CRT aggregation}\\
      \readout{A composite modulus may help by combining information from its factors.}\\
      For example, $x \bmod 96$ gives $x \bmod 2,4,8,16,32$\newline
      and $x \bmod 3$ at the same time.
      \par\vspace{1pt}

      \textbf{Next steps}\\
      \readout{This suggests testing more composite moduli.}
      \begin{itemize}\setlength{\itemsep}{0.3em}
        \item Test \alert{highly composite numbers beyond 101} before adding more primes.
      \end{itemize}
    \end{column}
  \end{columns}
\end{frame}

\begin{frame}{From Prediction to Mathematical Discovery}
\small
  \readout{We now return to our goal: using these representations to find mathematical connections.}
  \par\smallskip
  {\footnotesize\textbf{This paper:} prediction improvement and learned arithmetic structure.}
  \par\medskip
  \centering
  \begin{tikzpicture}[x=1cm,y=1cm,
    flow/.style={-{Stealth[length=2mm]},thick,draw=mDarkTeal},
    future/.style={flow,dashed,draw=mLightBrown},
    card/.style={rounded corners=3pt,draw=mDarkTeal!30,fill=mDarkTeal!4,
      text width=2.8cm,minimum height=0.58cm,align=center,font=\small}]
    \useasboundingbox (0,0) rectangle (14,4.45);
    \draw[rounded corners=3pt,dashed,draw=mLightBrown]
      (0.05,0.05) rectangle (13.95,4.4);
    \node[anchor=west,text=black,font=\footnotesize\bfseries] at (0.3,4.12)
      {Future direction: similarity retrieval and conjecture generation (not demonstrated here)};
    \node[card] (a) at (2,3.4) {Sequence A};
    \node[card] (b) at (2,2.5) {Sequence B};
    \node[card,fill=mDarkTeal,text=white] (ma) at (7,3.4) {IntSeqBERT};
    \node[card,fill=mDarkTeal,text=white] (mb) at (7,2.5) {IntSeqBERT};
    \node[card] (ea) at (12,3.4) {Embedding A};
    \node[card] (eb) at (12,2.5) {Embedding B};
    \draw[flow] (a) -- (ma);
    \draw[flow] (b) -- (mb);
    \draw[flow] (ma) -- (ea);
    \draw[flow] (mb) -- (eb);
    \draw[future] (ea.east) -- (13.7,3.4) -- (13.7,1.7) -- (12,1.7) -- (12,1.3);
    \draw[future] (eb.south) -- (12,1.3);
    \node[card,draw=mLightBrown,text width=3.5cm,font=\footnotesize,minimum height=1.05cm] (similar) at (12,0.8)
      {Similar embeddings\\{\footnotesize Arithmetic representations}};
    \node[card,draw=mLightBrown,text width=3.5cm,font=\footnotesize,minimum height=1.05cm] (connection) at (7,0.8)
      {Possible mathematical\\connection};
    \node[card,draw=mLightBrown,fill=mLightBrown!15,text width=3.5cm,font=\footnotesize,minimum height=1.05cm] (conjecture) at (2,0.8)
      {Candidate conjecture};
    \draw[future] (similar) -- (connection);
    \draw[future] (connection) -- (conjecture);
  \end{tikzpicture}
  \par\smallskip
  {\footnotesize\readout{Those connections could suggest conjectures. This remains a goal for \textbf{future work}.}}
\end{frame}

\begin{frame}{Positioning in Computer Mathematics}
\small
  \begin{itemize}\tightlist
    \item \readout{Finding related sequences could also help with program synthesis.}
    \item \readout{Program synthesis seeks a rule that generates a sequence.}
          Examples include \textbf{Alien Coding}, \textbf{QSynt}, and \textbf{Learning Conjecturing}.
    \item \readout{Related sequences could provide clues about which rules to try.}
          The representations are learned across the whole corpus.
      \begin{itemize}
        \item Synthesis asks: \emph{``What program generates this sequence?''}
        \item We ask: \emph{``Which sequences are number-theoretically similar,
              even when they are generated in different ways?''}
      \end{itemize}
    \item \readout{Adding this approach to systems such as Alien Coding might improve their accuracy.}
    \item Our code, preprocessing, and evaluation are available at
          \texttt{github.com/aabaa/IntSeqBERT}.
  \end{itemize}
\end{frame}

\begin{frame}{Conclusion}
\small
  \setlength{\parskip}{0pt}
  \begin{enumerate}\tightlist
    \item \readout{The modulo spectrum makes arithmetic structure visible in the input.}
          A \textbf{ring homomorphism} preserves multiplication \alert{at depth 0}.
    \item \readout{It improves prediction at every model size.}
          Exact next-term accuracy improves by \alert{7.4$\times$} over Vanilla.
    \item \readout{We observe a link between information gain and the totient ratio.}
          NIG is negatively correlated with $\varphi(m)/m$ ($r = -0.851$).
    \item \readout{Exact prediction remains difficult, especially for large integers.}
          This supports further work on \alert{neural-symbolic methods}.
  \end{enumerate}
  \medskip
  \readout{We will improve decoding and explore how to find related sequences.}\\
  We plan approximate CRT for large integers, highly composite moduli beyond 101,
  larger-scale retraining, and \alert{similarity retrieval \& conjecture generation}.
  \medskip
  \begin{center}
    \readout{Thank you for listening.} --- \texttt{github.com/aabaa/IntSeqBERT}
  \end{center}
\end{frame}

\appendix



\begin{frame}{Backup --- Why Not Digit-Based Tokenisation?}
\begin{itemize}\tightlist
    \item \readout{Another option is to generate integers 1 decimal digit at a time.}
          Each digit is a token. \readout{We have not compared this approach experimentally in this paper.}
    \item \readout{Our input gives the model residues directly.}
          We target the \alert{number-theoretic structure of the integer itself}.
          With digit tokens, the model must calculate residues from the digits.
    \item \readout{Digit-by-digit generation also changes the prediction task.}
          It predicts a sequence of digits, while our model predicts 1 integer value.
    \item We plan to compare the 2 settings under the same conditions.
  \end{itemize}
\end{frame}

\begin{frame}{Backup --- Limitations}
\begin{itemize}\tightlist
    \item \readout{Large integers remain difficult because residue predictions are uncertain.}
          In \textbf{13.4\%} of Solver calls, no valid candidate is found (\texttt{none} mode).
          Approximate CRT or less strict residue constraints may help.
    \item \readout{The dataset also affects our overall scores.}
          Many \texttt{nonn} sequences help sign accuracy;
          many slowly growing sequences help magnitude accuracy.
    \item \readout{Our experiments use limited compute and 1 random seed.}
          We use 1 \textbf{8\,GB GPU}, seed \textbf{42}, and a vocabulary of \textbf{20{,}003} tokens.
          Vanilla uses the \emph{FACT architecture} with our settings;
          we do not reproduce FACT's reported results.
  \end{itemize}
\end{frame}

\begin{frame}{Backup --- How Hard Is Multiplication for Neural Networks?}
\footnotesize
  \begin{itemize}\tightlist
    \item \readout{The difficulty of multiplication depends on the model and the training method.}
          In \textbf{circuit theory}, $n$-bit multiplication is not in AC$^0$
          (constant-depth AND/OR). Polynomial-size threshold circuits can do it with
          \alert{3 layers}, and 3 is optimal. Iterated products need only 4 layers
          (Siu \& Roychowdhury, \emph{SIAM J.\ Discrete Math.}, 1994).
    \item Log-precision \textbf{Transformers} are upper-bounded by uniform TC$^0$
          (Merrill \& Sabharwal 2023). This connects multiplication to model limits.
    \item \readout{Experiments show that learning multiplication can still be difficult.}
          GPT-4 zero-shot accuracy falls from \textbf{59\%} for $3{\times}3$-digit products
          to \alert{4\%} for $4{\times}4$-digit products
          (Dziri+, \emph{Faith and Fate}, NeurIPS 2023).
    \item A \textbf{2-layer Transformer} on modular arithmetic generalises only after a
          very long training period: \textbf{grokking} (Power+ 2022, arXiv:2201.02177).
    \item \textbf{Shen et al.\ (2023)} report \textbf{84\%} accuracy on
          $15{\times}15$-digit multiplication with a \textbf{12-layer Transformer},
          zero-padding, and reversed output digits (arXiv:2311.14737, Sec.\ 2 / Table 5).
          This does not establish a minimum layer count.
    \item \textbf{Bai et al.\ (2025)} study a \textbf{2-layer} model on $4{\times}4$-digit
          multiplication: implicit-CoT training succeeds where standard fine-tuning fails.
          Their analysis finds a tree-like attention mechanism (arXiv:2510.00184).
  \end{itemize}
\end{frame}


\begin{frame}[label=backup-oeis-representation]{Backup --- OEIS Tags: Number Representations}
\small
\setlength{\parskip}{0pt}
\readout{These tags describe digits, fractions, or language-based rules. We leave them outside the scope of this study.}
\par\medskip
{\footnotesize
\renewcommand{\arraystretch}{1.35}
\begin{tabular}{@{}p{0.9cm}p{5.0cm}p{7.0cm}@{}}
\toprule
\textbf{Tag} & \textbf{Meaning} & \textbf{Example} \\
\midrule
\texttt{cons} & Digits of a constant, usually in base 10.
 & $\pi=3.14159\ldots$ gives $3,1,4,1,5,9,\ldots$ \\
\texttt{cofr} & Coefficients in a continued fraction.
 & $\pi=[3;7,15,1,292,\ldots]$ gives $3,7,15,1,292,\ldots$ \\
\texttt{frac} & Numerators or denominators of a sequence of fractions.
 & For $1/2,\,2/3,\,3/4,\ldots$, the denominators are $2,3,4,\ldots$ \\
\texttt{base} & A rule that depends on the number base.
 & Sum of decimal digits: $19\mapsto 1+9=10$. \\
\texttt{word} & A rule that depends on words in a language.
 & Number of letters in English number names: ``one'', ``two'', ``three'' give $3,3,5$. \\
\bottomrule
\end{tabular}}
\par\medskip
\readout{These sequences can have rich mathematical structure. Excluding them is a choice about our task.}
\par\smallskip
{\scriptsize Source: \href{https://oeis.org/wiki/Keywords}{OEIS keyword definitions}. Examples illustrate each type.}
\end{frame}

\begin{frame}{Backup --- OEIS Tags: Task Scope and Data Selection}
\small
\setlength{\parskip}{0pt}
\readout{We also exclude finite sequences, tables, and entries with the following labels.}
\par\medskip
{\footnotesize
\renewcommand{\arraystretch}{1.25}
\begin{tabular}{@{}p{0.9cm}p{5.0cm}p{7.0cm}@{}}
\toprule
\textbf{Tag} & \textbf{Meaning} & \textbf{Why we exclude it / what to keep in mind} \\
\midrule
\texttt{fini} & A sequence known to be finite.
 & There is no next term after its endpoint. \\
\texttt{tabl} & A regular table or triangle read row by row.
 & For example, Pascal's triangle. Row boundaries add structure beyond a simple list. \\
\texttt{dead} & An incorrect or duplicate entry.
 & We exclude known errors and duplicates. \\
\texttt{unkn} & The definition or context is unknown.
 & We exclude entries whose meaning is unclear. \\
\texttt{less} & An entry judged less interesting.
 & A subjective OEIS label; it does not mean the sequence is incorrect. \\
\texttt{dumb} & An entry judged unimportant.
 & Also a subjective label, not a mathematical test. \\
\bottomrule
\end{tabular}}
\par\medskip
\readout{These filters define our dataset. Other studies may include these sequences.}
\par\smallskip
{\scriptsize Source: \href{https://oeis.org/wiki/Keywords}{OEIS keyword definitions}. Selection reasons describe our study.}
\end{frame}

\end{document}
